name: 'CI'
on:
  pull_request:
    branches:
      - main
      - release/**
  push:
    branches:
      - main
      - release/**

jobs:
  build:
    name: Build RPMs
    secrets: inherit
    uses: ./.github/workflows/build.yaml
